# Integers are whole numbers

# Store the number 42 in the variable `age`
age = ?
print(age)

# In python, integers can be extremely large
# without causing problems
# Store a very larger integer in the variable `big`.
big = ?
print(big)

# The double-division operator rounds down to
# the nearest whole number
# Divide 7 by 3, storing the whole number portion of the result in `divided`
divided =
print(divided) # prints 2

# Conver the string "7" to an integer 7 and store the result 
# in the variable `favorite`.
favorite = ?
print(favorite)
