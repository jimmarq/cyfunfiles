
# Main Methods

# 1) Define a function called `greet` that takes in a `name` parameter.
# Inside the function, print a personalized greeting message to the console using the `name` parameter.


# 2) Add a `main` method
# The main method should call the `greet` function with your name as an argument.
