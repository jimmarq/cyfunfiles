# Nested Lists

# Create an empty list with the variable
# name `container`

# Add a list element to `container` 
# with three strings.

# Add a list element to `container` 
# with three integers.

# Add a list element to `container` 
# with one string and one integer.

# Print `container`

# Print the number of elements in
# `container`
